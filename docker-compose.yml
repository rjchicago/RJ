services:
  rj:
    image: rjchicago/rj:dev
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - ./web:/app
      - /app/node_modules
    restart: unless-stopped
